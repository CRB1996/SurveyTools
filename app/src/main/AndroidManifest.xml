<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chen.surveytools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.autofocus" />
    <!-- 震动 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ico"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 主界面 -->
        <activity android:name=".MenuActivity" />
        <activity android:name=".FlashlampActivity" />
        <activity android:name=".CompassActivity" />
        <activity android:name=".WatchActivity" />
        <activity
            android:name="com.chen.ruler.RuleActivity"
            android:label="@string/title_activity_rule" />
        <activity android:name="com.chen.ruler.CycleRulerActivity" />
        <activity android:name="com.chen.level.LevelActivity" />
        <activity android:name="com.chen.GPS.GPSActivity" />
        <activity android:name="com.chen.member.MemberActivity"/>
    </application>

</manifest>